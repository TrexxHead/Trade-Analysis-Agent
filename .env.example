DERIV_API_TOKEN=
DERIV_APP_ID=1089

METAAPI_TOKEN=
METAAPI_ACCOUNT_ID=

# Only needed if exposing the dashboard beyond 127.0.0.1 (e.g. via a tunnel).
# Leave blank for pure-localhost use.
DASHBOARD_PASSWORD=
